import { PassCheck } from './pass-check';

describe('PassCheck', () => {
  it('should create an instance', () => {
    const directive = new PassCheck();
    expect(directive).toBeTruthy();
  });
});
