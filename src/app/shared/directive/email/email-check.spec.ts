import { EmailCheck } from './email-check';

describe('EmailCheck', () => {
  it('should create an instance', () => {
    const directive = new EmailCheck();
    expect(directive).toBeTruthy();
  });
});
